name: manage_user_bloc
description: A new Flutter project.

#link: https://raw.githubusercontent.com/JohannesMilke/futurebuilder_example/master/assets/users.json

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #bloc
  flutter_bloc: ^7.1.0
  equatable: ^2.0.3
  #loader_overlay:
  loader_overlay: ^2.0.5+1
  #http:
  http:
  #adaptive_dialog:
  adaptive_dialog: ^1.3.0


  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

